#pragma once
#include <iostream>
#include <vector>
#include <map>
#include <string>

using namespace std;

void PrimeNum(size_t N,vector<int>* primeVector);
map<int,int> NumWords(string* text);